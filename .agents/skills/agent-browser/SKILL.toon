N:agent-browser
D:Browser automation CLI for AI agents. Use refs from snapshot -i for deterministic interaction, verification, and repeatable web workflows.
G:agent-browser browser-automation ai-agent web-testing scraping snapshot-refs verification sessions
U[5]:
  Open websites and automate interactions
  Fill forms and verify expected UI changes
  Capture screenshots and extract structured signals
  Run deterministic ref-based browser workflows
  Isolate parallel tasks with named sessions
S[6]{n,action,details}:
  1,Open,Navigate using agent-browser open <url>
  2,Wait,Stabilize page with wait --load networkidle or selector wait
  3,Snapshot,Collect refs via agent-browser snapshot -i
  4,Interact,Use @eN refs for click/fill/select/check
  5,Resnapshot,Run snapshot -i again after navigation or DOM updates
  6,Verify,Use diff snapshot or screenshot baseline diff for evidence
R[4]:
  Never reuse stale refs after page changes
  Prefer explicit waits over fixed sleep
  Use eval --stdin for complex JavaScript payloads
  Close sessions or use unique --session names in parallel runs
E[2]{desc,in,out}:
  "Click and verify","snapshot -i; click @e2; diff snapshot","Accessibility tree diff confirms expected change"
  "Visual regression check","screenshot baseline.png; click @e5; diff screenshot --baseline baseline.png","Mismatch and changed regions reported"
