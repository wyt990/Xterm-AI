N:jeo
D:[jeo] 통합 AI 오케스트레이션. ralph+plannotator 계획→team/bmad 실행→agent-browser 브라우저 검증(agentui=agentation watch loop)→worktree 자동 정리. Claude/Codex/Gemini/OpenCode 전 플랫폼 지원. 설치: omc omx ohmg bmad ralph plannotator agent-browser agentation.
G:jeo orchestration ralph plannotator agentation agentui team bmad omc omx ohmg agent-browser multi-agent workflow worktree cleanup browser-verification ui-feedback 오케스튤 계획 실행 검증 정리

U[5]:
  멀티에이전트 작업의 계획→실행→브라우저검증→UI피드백(agentui)→정리 전체 플로우 자동화
  ralph+plannotator로 코딩 시작 전 계획 시각적 검토 및 승인
  team(Claude) 또는 bmad(fallback)로 병렬 에이전트 실행
  agent-browser로 브라우저 기반 동작 검증 (스냅샷·스크린샷·UI 확인) — 기본 동작
  Claude/Codex/Gemini/OpenCode 모두에서 동일한 워크플로우 플러그인 실행

S[8]{n,action,details}:
  1,Install,bash scripts/install.sh --all (ralph omc omx ohmg bmad plannotator agent-browser 전체 설치)
  2,Setup-Claude,bash scripts/setup-claude.sh (/plugin install oh-my-claudecode + plannotator 훅 설정)
  3,Setup-Codex,bash scripts/setup-codex.sh (~/.codex/config.toml developer_instructions + /prompts:jeo)
  4,Setup-Gemini,bash scripts/setup-gemini.sh (~/.gemini/settings.json 훅 + GEMINI.md 지시사항)
  5,Setup-OpenCode,bash scripts/setup-opencode.sh (opencode.json 플러그인 등록 + slash commands)
  6,Plan,ralph+plannotator 계획수립: /ralph "jeo-plan: <task>" → plannotator 블로킹 실행(& 금지) → Approve("approved":true) or Feedback → 재계획 루프
  7,Execute,team 우선: /omc:team 3:executor "<task>" / team 없음: /workflow-init (BMAD)
  8,Verify-Cleanup,agent-browser snapshot <url> → UI 검증 → agentui 시 agentation watch loop(ack→fix→resolve) → bash scripts/worktree-cleanup.sh

R[5]:
  ralph+plannotator로 계획 먼저 검토 — 코딩 시작 전 잘못된 방향 조기 차단
  plannotator는 반드시 블로킹 실행(& 금지) — "approved":true 확인 후 EXECUTE 진입 / 피드백 시 재계획 루프
  agentui(agentation) 동작: agentation_watch_annotations 블로킹 → annotation ack→fix→resolve 루프 / HTTP 폴백(Codex/Gemini/OpenCode)
  Claude+omc: team 우선 / Codex·Gemini·OpenCode: bmad fallback 사용
  브라우저 관련 작업 완료 시 agent-browser snapshot으로 UI 동작 검증 (기본 동작)
  상태는 {worktree}/.omc/state/jeo-state.json에 세션 간 저장

E[3]{desc,in,out}:
  "전체 설치","bash scripts/install.sh --all && bash scripts/check-status.sh","모든 컴포넌트 설치 완료 + 전체 AI 툴 연동 확인"
  "계획→실행→검증","jeo '새 기능 구현' → ralph 계획 → Approve → team 실행 → agent-browser snapshot","브라우저 동작 검증 완료 스크린샷 저장"
  "정리","bash scripts/worktree-cleanup.sh","git worktree prune 완료"
